spring:
  cloud:
    config:
      discovery:
        service-id: config-server
      fail-fast: true
      name: config-client
      profile: developer
      username: admin
      password: qwerty
      retry:
        max-attempts: 1
        max-interval: 1000